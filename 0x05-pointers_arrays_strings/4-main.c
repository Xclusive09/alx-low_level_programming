#include "main.h"

/**
 * print_rev - print a string in reverse
 * @s: pointeer to the string
 * Return: Always 0
 */
void print_rev(char *s)
{
char *str;
str = "I do not fear computers. I fear the lack of them - Isaac Asimov";

print_rev(str);
return (0);
}

